package com.bridgelabz.week3;

import java.sql.Date;
import java.text.SimpleDateFormat;

import utility.Utility;

public class RegularExpression {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		System.out.println("Enter the name");
		String name = Utility.inputString();
		String [] x = name.split("");
		String fname = x[0];
		System.out.println(x);
		System.out.println("Enter the contact number");
		String n = Utility.inputString();
		
		SimpleDateFormat DateFormat = new SimpleDateFormat("DD/MM/YYYY");
		Date date = new Date();
		String today = DateFormat.format(date);
		System.out.println(today);
		
		String template = "Hello <<name>>, We have your full name\n" + 
				"as <<full name>> in our system. your contact number is 91­xxxxxxxxxx. Please,let us\n" + 
				"know in case of any clarification Thank you BridgeLabz 01/01/2016.";
		
		
		String regexName = "<<name>>";
		String regexFullName = "<<fname>>";
		String regexPhoneNumber = "xxxxxxxxxx";
		String regexDate = "DD/MM/YYYY";
		
		String template = Utility.regexpression(template, regexName);
		String template = Utility.regexpression(template,regexFullName);
		String template = Utility.regexpression(template,regexPhoneNumber);
		String template = Utility.regexpression(template,regexDate);



	}

}
